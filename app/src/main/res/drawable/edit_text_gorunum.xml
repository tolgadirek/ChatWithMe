<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FFFFFF"/> <!-- İç renk, beyaz -->
    <stroke android:color="#000000" /> <!-- Kenar rengi (Örneğin, mor) -->
    <stroke android:width="2dp"/>    <!-- Kenar genişliği -->
    <corners android:radius="8dp"/> <!-- Kenar yuvarlama (isteğe bağlı) -->
</shape>